@model IEnumerable<SolutionE_RH_v1._5.Models.DEMANDE_CONGE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DATE_ENVOI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERIODE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DATE_DEBUT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FICHIER_JUST)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EMPLOYEE.CIN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DATE_ENVOI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERIODE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DATE_DEBUT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FICHIER_JUST)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EMPLOYEE.CIN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DEMANDE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DEMANDE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DEMANDE })
        </td>
    </tr>
}

</table>
